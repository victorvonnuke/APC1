#include <stdio.h>
int main(){
    int numeros [10]
    int numero

    printf("Digite o numero: ");
    scanf("%i", numero[i]);
 
    for(int i=0; i<10; i++) {
        printf("Digite o numero que vai se repetir: ");
        scanf("%i", numeros [i]);
        getchar

        if (numeros[i] == numero) {
            achou = i;
          }
          
    }
    
}